module.exports = function(store){

  // store.query.expose('users', 'byEmail', function(email){
  //   return this.where('data.email').equals(email)
  // })

  // store.queryAccess('users', 'byEmail', function(email, accept, onErr){
  //   accept(true)
  // })
}